git clone https://github.com/naifehrarli/capstone.git //to load it

pip install -r requirements.txt //to install needed dependencies (there are new ones cuz of the pfp thing)

git pull //to load the latest version of the code
git push //to save your changes to the code
